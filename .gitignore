# =========================================================
# COMMON: Dependencies / Build outputs / Caches
# =========================================================
node_modules/
dist/
build/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
Thumbs.db

# Logs
*.log

# =========================================================
# ENV / SECRETS (Never commit)
# =========================================================
# Root env variants
.env*
!.env.example

# Backend env variants
backend/.env*
!backend/.env.example

# Frontend env variants
frontend/.env*
!frontend/.env.example

# =========================================================
# BACKEND: Local artifacts / Secrets
# =========================================================
backend/assistant.db
backend/logs/
backend/secrets/
backend/client_secrets.json
backend/credentials.json
backend/keys.json
backend/secret_keys.json

# Protects the dynamic token vault from being pushed to GitHub
backend/data/credentials/*.json

# Legacy backup — contains stale src.* references, excluded from Docker too
backend/src_backup/

# =========================================================
# FRONTEND: Build artifacts
# =========================================================
frontend/dist/
frontend/.next/

# =========================================================
# PROJECT: Session/status files that may contain secrets
# =========================================================
SESSION_STATUS.md
*_SECRETS.md
*.secret.md

# =========================================================
# LOCAL DIAGNOSTICS / SCRATCH (Never commit)
# =========================================================
non_ascii_scan.txt
test_e3c.ps1
# HYG-01: Local deterministic artifacts (do not commit)
apply_*.py
*.diff.txt
*.patch
*.bak.*
emails.json
data/


# Documentation files (keep local only)
*_DEPLOYMENT.md
CRITICAL_FIXES_DEPLOYMENT.md
SECURITY_FIX_DEPLOYMENT.md
